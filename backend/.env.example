PORT=5000
MONGO_URI=mongodb://localhost:27017/fitness_tracker
JWT_SECRET=change_this_to_a_long_random_secret
JWT_EXPIRES_IN=7d
CLIENT_URL=http://localhost:5173
NODE_ENV=development
# Optional email (for forgot password) - if not set, reset token is returned in API response (dev mode)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
